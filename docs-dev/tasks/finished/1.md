Title: 1
Description: Task from 1.md
Prompt:
优化 test.ps1, test.sh 脚本.

当前版本接口：

```bash
Usage: test.ps1 [test-type] [maven-args...]

Test Types:
  fast        Run fast unit tests only
  it          Run integration tests
  e2e         Run end-to-end tests
  sdk         Run SDK tests
  python-sdk  Run Python SDK tests
  nodejs-sdk  Run NodeJS SDK tests
  core        Run core module supplementary tests
  rest        Run REST module tests
  all         Run all tests (integration, e2e, sdk)
```

优化建议：

## 1.
test.ps1 sdk
修改为
test.ps1 kotlin-sdk

## 2.

支持组合：

```bash
test.ps1 it sdk
```

## 3.

test.ps1 all
行为对齐
test.ps1 fast core it e2e rest

